# =====================================================
# INVOKE CONFIGURATION: invoke-cleanup
# =====================================================
# -- ON WINDOWS:
# run:
#   echo: true
#   pty:  false
#   shell: C:\Windows\System32\cmd.exe
# =====================================================
# MAYBE: tasks: auto_dash_names: false

run:
    echo: true

cleanup:
    extra_directories:
      - "build"
      - "dist"

cleanup_all:
    extra_directories:
      - .hypothesis
      - .pytest_cache
      - .idea
